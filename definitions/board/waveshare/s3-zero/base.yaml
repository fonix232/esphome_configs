---
esphome:
  platformio_options:
    board_build.flash_mode: dio

esp32:
  flash_size: 4MB

psram:
  mode: quad
  speed: 40MHz

packages:
  base: !include ../../../base.yaml
  extended: !include ../../../extended.yaml