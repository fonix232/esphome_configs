---
ch422g:
  - id: ch422g_hub

output:
  - platform: gpio
    pin: 
      ch422g: ch422g_hub
      number: ${display_backlight_pin}
      mode:
        output: true
      inverted:  ${display_backlight_pin_inverted}
    id: display_backlight_binary